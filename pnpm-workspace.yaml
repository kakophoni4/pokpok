packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks lifecycle scripts by default; these need them to fetch engines/binaries.
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - prisma
  - esbuild
